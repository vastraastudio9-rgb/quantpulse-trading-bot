# Security policy

Do not report credentials or account details in a public issue. Revoke any
secret immediately if it is accidentally exposed and remove it from Git
history before continuing.

This repository must contain placeholders only. Runtime secrets belong in an
untracked `.env` file or a deployment secret manager.
